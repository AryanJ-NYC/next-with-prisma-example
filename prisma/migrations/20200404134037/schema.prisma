datasource db {
  provider = "sqlite"
  url = "***"
}

generator client {
  provider = "prisma-client-js"
}

model Movie {
  id           Int    @default(autoincrement()) @id
  director     String
  movieName    String
  yearReleased String
}
